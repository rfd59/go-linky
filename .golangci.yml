version: "2"

linters:    
  settings:
    errcheck:
      exclude-functions:
        - (go.bug.st/serial.Port).Close

# formatters:
#   enable:
#     - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
